#include "../include/config.hpp"
const float particleRadius = 2;
const float thresholdRadius = 100;
const float cellSize = 100;
const float particleEquilibrium = 0.1;
const float delta_t = 0.001;
// const float friction = pow(0.5, delta_t / 0.01);
const float friction = 0.6;

